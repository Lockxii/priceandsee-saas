fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.11.5
parsel==1.9.1
# Keep Railway boot light. Heavy browser/agent providers are optional and imported lazily.
# For local browser providers, install only what you enable:
#   pip install crawl4ai camoufox browser-use langchain-openai
